import { FocusableInputDirective } from './focusable-input.directive';

describe('FocusableInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusableInputDirective();
    expect(directive).toBeTruthy();
  });
});
